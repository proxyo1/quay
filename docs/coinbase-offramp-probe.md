# Coinbase CDP Offramp — Phase 0 probe

Generated by `scripts/coinbase-offramp-probe.ts`. Read-only; no funds moved, no order committed.

- Host: `api.developer.coinbase.com`
- Credential source: `frontend/.env.local`
- Probe address: `0x2084bbffd0e742beff5e07f587a84d372326b2ef40524762ae96d64d42bd0c57`
- Corridor probed: USDC on sui → SGD in SG

## Verdict

**INCONCLUSIVE** — no hard failure, but not every gate is green.


| # | Check | Verdict | Detail |
|---|---|---|---|
| 1 | GET /sell/config | ✓ PASS | SG supported. payment_methods = [CRYPTO_ACCOUNT, FIAT_WALLET] (no bank rail) |
| 2 | SGD in cashout_currencies | ✓ PASS | SGD payable. 7 currencies offered. |
| 3 | sui in USDC networks | ✓ PASS | USDC sellable from Sui. contract_address=0xdba34672e30cb065b1f93e3ab55318768fd6fef66c15942c9f7cb846e2f900e7::usdc::USDC |
| 4 | POST /token accepts a Sui address | ✓ PASS | session token minted (48 chars, single-use, ~5 min) |
| 5 | POST /sell/quote prices the order | ✓ PASS | quote_id=bc67c1e1-f029-42c6-9163-ffd1500e1e44 coinbase_fee={"value":"0.13","currency":"SGD","cbrn":""} cashout_total={"value":"12.67","currency":"SGD","cbrn":""} |
| 5.1 | offramp_url populated by the API? | ? INCONCLUSIVE | API returns an EMPTY STRING in all request shapes ⇒ construct the widget URL ourselves |
| 6 | constructed pay.coinbase.com URL is live | ✓ PASS | GET /v3/sell/input → HTTP 200 |

## Still open after this probe

- **Does the widget issue a real Sui deposit address for a live SG account?**
  Not answerable from the CLI — the deposit address does not exist until a merchant
  commits an order inside `pay.coinbase.com`.
- **Does offramp need separate production approval?** The quickstart lists only
  account + secret key + domain allowlist, but that is not the same as confirmation.
- **`from_address` semantics** — does Coinbase validate the transaction *sender*
  or the *gas payer*? Quay's sponsored PTB has `sender = merchant`,
  `gasOwner = sponsor`, so the two differ.

## Blocker found while probing (unrelated to Coinbase)

Sui has **retired JSON-RPC on the public fullnodes**, on mainnet *and* testnet.
Every method returns:

> `-32601 Method not found. JSON-RPC on public fullnodes has been deprecated.
> Please migrate to gRPC or GraphQL endpoints.`

Verified by raw `curl` and through `@mysten/sui` 2.16.2 for
`getCoinMetadata`, `getCoins`, `getBalance`, `getObject`, `getOwnedObjects`,
`getDynamicFields`, `devInspectTransactionBlock`, `getLatestSuiSystemState`
and `queryEvents` — all nine fail.

Quay builds every client as
`new SuiClient({ network: SUI_NETWORK, url: getFullnodeUrl(SUI_NETWORK) })`
(`getJsonRpcFullnodeUrl("mainnet")` → `https://fullnode.mainnet.sui.io:443`)
with no env override, so **all on-chain reads and writes are currently down**,
not just the offramp. The SDK ships `@mysten/sui/grpc` and
`@mysten/sui/graphql` as the migration targets.

Consequence for this rail: the plan's dry-run verification steps (swap
`devInspect`, yield-path dry-run, the live capped run) cannot execute until the
transport is migrated, and the offramp's own session route depends on the same
reads (registry lookup, coin collection, Scallop balance sheet).

## Raw evidence

### Check 1 — GET /sell/config

```json
{
  "id": "SG",
  "subdivisions": [],
  "payment_methods": [
    {
      "id": "CRYPTO_ACCOUNT"
    },
    {
      "id": "FIAT_WALLET"
    }
  ]
}
```

### Check 2 — SGD in cashout_currencies

```json
[
  "AUD",
  "BRL",
  "CAD",
  "EUR",
  "GBP",
  "SGD",
  "USD"
]
```

### Check 3 — sui in USDC networks

```json
{
  "networks": [
    "hyperliquid",
    "monad",
    "ethereum",
    "arbitrum",
    "avacchain",
    "base",
    "noble",
    "optimism",
    "polygon",
    "solana",
    "stellar",
    "sui"
  ],
  "sui": {
    "name": "sui",
    "display_name": "Sui",
    "contract_address": "0xdba34672e30cb065b1f93e3ab55318768fd6fef66c15942c9f7cb846e2f900e7::usdc::USDC",
    "chain_id": "0",
    "icon_url": "https://asset-metadata-service-production.s3.amazonaws.com/asset_icons/f0a4d29359ff3bb5db86f925b2ff477a570bf27a3d7b5a5af5859953637e216b.png"
  }
}
```

### Check 4 — POST /token accepts a Sui address

```json
{
  "tokenLength": 48,
  "channelId": ""
}
```

### Check 5 — POST /sell/quote prices the order

```json
{
  "cashout_total": {
    "value": "12.67",
    "currency": "SGD",
    "cbrn": ""
  },
  "cashout_subtotal": {
    "value": "12.8",
    "currency": "SGD",
    "cbrn": ""
  },
  "sell_amount": {
    "value": "10",
    "currency": "USDC",
    "cbrn": ""
  },
  "coinbase_fee": {
    "value": "0.13",
    "currency": "SGD",
    "cbrn": ""
  },
  "quote_id": "bc67c1e1-f029-42c6-9163-ffd1500e1e44",
  "offramp_url": ""
}
```

### Check 5.1 — offramp_url populated by the API?

```json
{
  "offramp_url": ""
}
```

### Check 6 — constructed pay.coinbase.com URL is live

```json
{
  "path": "/v3/sell/input",
  "status": 200
}
```


### Additional

```json
{
  "usdcContractAddressFromCdp": "0xdba34672e30cb065b1f93e3ab55318768fd6fef66c15942c9f7cb846e2f900e7::usdc::USDC",
  "widgetUrlPattern": "https://pay.coinbase.com/v3/sell/input",
  "quoteSample": {
    "cashout_total": {
      "value": "12.67",
      "currency": "SGD",
      "cbrn": ""
    },
    "cashout_subtotal": {
      "value": "12.8",
      "currency": "SGD",
      "cbrn": ""
    },
    "sell_amount": {
      "value": "10",
      "currency": "USDC",
      "cbrn": ""
    },
    "coinbase_fee": {
      "value": "0.13",
      "currency": "SGD",
      "cbrn": ""
    },
    "quote_id": "bc67c1e1-f029-42c6-9163-ffd1500e1e44",
    "offramp_url": ""
  }
}
```
